# pwr4exp 1.0.1

## Bug fixes

-   Improved handling of the `correlation` argument in `mkdesign()`. Fixed an issue where correlation structures failed when passed through variables — they now work correctly when supplied as variable inputs instead of direct expressions.

# pwr4exp 1.0.0

# pwr4exp 0.1.0
