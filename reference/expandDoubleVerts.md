# Expand terms with `'||'` notation into separate `'|'` terms

Expand terms with `'||'` notation into separate `'|'` terms

## Usage

``` r
expandDoubleVerts(term)
```

## Arguments

- term:

  a formula

## Value

the modified term
